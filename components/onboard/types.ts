// import type { CallBackProps, STATUS, Step } from 'react-joyride';

// export interface OnboardingStep extends Step {
//   // id: string; // Unique ID per step
//   group?: string; // Optional group (like "imageGeneration")
//   path?: string; // URL path for step
//   condition?: {
//     field: string;
//     operator: '==' | '!=' | '>' | '<' | '>=' | '<=';
//     value: any;
//   }; // Conditional logic per step
// }
